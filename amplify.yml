version: 1

frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24
        - node --version
        - npm --version
        - HUSKY=0 npm ci --cache .npm --prefer-offline

    build:
      commands:
        - npm run build

  artifacts:
    baseDirectory: .next
    files:
      - '**/*'

  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
