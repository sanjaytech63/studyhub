version: 1

applications:
  - appRoot: apps/web

    frontend:
      buildPath: /

      phases:
        preBuild:
          commands:
            - nvm use 24
            - node --version
            - npm --version
            - HUSKY=0 npm ci --cache .npm --prefer-offline

        build:
          commands:
            - npm run build:web

      artifacts:
        baseDirectory: apps/web/.next
        files:
          - '**/*'

      cache:
        paths:
          - .npm/**/*
